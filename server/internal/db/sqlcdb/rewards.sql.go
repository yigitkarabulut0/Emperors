// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: rewards.sql

package sqlcdb

import (
	"context"
	"time"

	"github.com/google/uuid"
)

const creditFavour = `-- name: CreditFavour :one
UPDATE app.players SET kingdom_favour = kingdom_favour + $1::bigint
WHERE id = $2
RETURNING id, username, display_name, level, xp, gold, treasury_gold, diamonds, energy_milli, energy_updated_at, stat_energy, stat_attack, stat_defense, stat_points_unspent, shield_until, action_seq, state, reset_offset_minutes, created_at, last_seen_at, soldier_slots, free_slot_claimed, free_recruit_claimed, is_bot, tax_milli_accrued, tax_updated_at, kingdom_id, kingdom_role, kingdom_joined_at, kingdom_donated_total, kingdom_favour, kingdom_rep_today, kingdom_donated_today, kingdom_day, avatar, tax_milli_per_hour, tax_unlogged, luck_bp, luck_expires_at, xp_boost_bp, xp_boost_expires_at, daily_streak, daily_claimed_on, might, legacy, kingdom_left_at, diamond_debt, refills_day, refills_used, boost_until, cos_frame, cos_title, cos_color, cos_crest, mail_bc_seen, vip_points, patron_until, steward_owned, bag_bonus, stipend_until, stipend_claimed, stipend_ref, vip_gift_on, shop_rerolls_day, shop_rerolls_used, storehouse_milli, storehouse_at, storehouse_cap_milli, cart_stock, cart_at, carts_opened, calendar_pos, calendar_cycle, frenzy_meter_milli, frenzy_last_at, frenzy_until, frenzy_energy_left, frenzy_day, frenzy_used, frenzy_ready_at, road_claimed, guide_step, guide_done_at, guide_skipped, winback_at, winback_tier, arena_day, arena_fights_used, arena_refresh_used, arena_first_win_on, bounty_day, bounty_placed, chat_muted_until, chat_strikes, chat_strike_at, chat_rules_version, chat_seen_seq, gift_day, gifts_taken, friend_req_day, friend_reqs, spy_day, spy_used, aid_day, aid_given, aid_asked_at, notif_raid, notif_chat, notif_mail, notif_events, notif_friends, quiet_from, quiet_to, privacy_profile, privacy_online, privacy_requests, talent_respecs
`

type CreditFavourParams struct {
	Favour int64
	ID     uuid.UUID
}

func (q *Queries) CreditFavour(ctx context.Context, arg CreditFavourParams) (AppPlayer, error) {
	row := q.db.QueryRow(ctx, creditFavour, arg.Favour, arg.ID)
	var i AppPlayer
	err := row.Scan(
		&i.ID,
		&i.Username,
		&i.DisplayName,
		&i.Level,
		&i.Xp,
		&i.Gold,
		&i.TreasuryGold,
		&i.Diamonds,
		&i.EnergyMilli,
		&i.EnergyUpdatedAt,
		&i.StatEnergy,
		&i.StatAttack,
		&i.StatDefense,
		&i.StatPointsUnspent,
		&i.ShieldUntil,
		&i.ActionSeq,
		&i.State,
		&i.ResetOffsetMinutes,
		&i.CreatedAt,
		&i.LastSeenAt,
		&i.SoldierSlots,
		&i.FreeSlotClaimed,
		&i.FreeRecruitClaimed,
		&i.IsBot,
		&i.TaxMilliAccrued,
		&i.TaxUpdatedAt,
		&i.KingdomID,
		&i.KingdomRole,
		&i.KingdomJoinedAt,
		&i.KingdomDonatedTotal,
		&i.KingdomFavour,
		&i.KingdomRepToday,
		&i.KingdomDonatedToday,
		&i.KingdomDay,
		&i.Avatar,
		&i.TaxMilliPerHour,
		&i.TaxUnlogged,
		&i.LuckBp,
		&i.LuckExpiresAt,
		&i.XpBoostBp,
		&i.XpBoostExpiresAt,
		&i.DailyStreak,
		&i.DailyClaimedOn,
		&i.Might,
		&i.Legacy,
		&i.KingdomLeftAt,
		&i.DiamondDebt,
		&i.RefillsDay,
		&i.RefillsUsed,
		&i.BoostUntil,
		&i.CosFrame,
		&i.CosTitle,
		&i.CosColor,
		&i.CosCrest,
		&i.MailBcSeen,
		&i.VipPoints,
		&i.PatronUntil,
		&i.StewardOwned,
		&i.BagBonus,
		&i.StipendUntil,
		&i.StipendClaimed,
		&i.StipendRef,
		&i.VipGiftOn,
		&i.ShopRerollsDay,
		&i.ShopRerollsUsed,
		&i.StorehouseMilli,
		&i.StorehouseAt,
		&i.StorehouseCapMilli,
		&i.CartStock,
		&i.CartAt,
		&i.CartsOpened,
		&i.CalendarPos,
		&i.CalendarCycle,
		&i.FrenzyMeterMilli,
		&i.FrenzyLastAt,
		&i.FrenzyUntil,
		&i.FrenzyEnergyLeft,
		&i.FrenzyDay,
		&i.FrenzyUsed,
		&i.FrenzyReadyAt,
		&i.RoadClaimed,
		&i.GuideStep,
		&i.GuideDoneAt,
		&i.GuideSkipped,
		&i.WinbackAt,
		&i.WinbackTier,
		&i.ArenaDay,
		&i.ArenaFightsUsed,
		&i.ArenaRefreshUsed,
		&i.ArenaFirstWinOn,
		&i.BountyDay,
		&i.BountyPlaced,
		&i.ChatMutedUntil,
		&i.ChatStrikes,
		&i.ChatStrikeAt,
		&i.ChatRulesVersion,
		&i.ChatSeenSeq,
		&i.GiftDay,
		&i.GiftsTaken,
		&i.FriendReqDay,
		&i.FriendReqs,
		&i.SpyDay,
		&i.SpyUsed,
		&i.AidDay,
		&i.AidGiven,
		&i.AidAskedAt,
		&i.NotifRaid,
		&i.NotifChat,
		&i.NotifMail,
		&i.NotifEvents,
		&i.NotifFriends,
		&i.QuietFrom,
		&i.QuietTo,
		&i.PrivacyProfile,
		&i.PrivacyOnline,
		&i.PrivacyRequests,
		&i.TalentRespecs,
	)
	return i, err
}

const extendBoostUntil = `-- name: ExtendBoostUntil :exec
UPDATE app.players
SET boost_until = GREATEST(coalesce(boost_until, $1::timestamptz), $1::timestamptz)
WHERE id = $2
`

type ExtendBoostUntilParams struct {
	Until time.Time
	ID    uuid.UUID
}

// Keeps the player row's "a boost is live until" marker at the latest expiry, so
// loadEffects reads the boost table only while one can still be live.
func (q *Queries) ExtendBoostUntil(ctx context.Context, arg ExtendBoostUntilParams) error {
	_, err := q.db.Exec(ctx, extendBoostUntil, arg.Until, arg.ID)
	return err
}

const insertCosmetic = `-- name: InsertCosmetic :one
INSERT INTO app.player_cosmetics (player_id, cosmetic_id, source, source_ref, expires_at)
VALUES ($1, $2, $3, $4,
        $5)
ON CONFLICT (player_id, cosmetic_id) DO NOTHING
RETURNING player_id, cosmetic_id, source, source_ref, acquired_at, expires_at
`

type InsertCosmeticParams struct {
	PlayerID   uuid.UUID
	CosmeticID string
	Source     string
	SourceRef  *string
	ExpiresAt  *time.Time
}

// Grants a cosmetic. Zero rows back means the player already owns it (a
// duplicate, which pays diamonds instead).
func (q *Queries) InsertCosmetic(ctx context.Context, arg InsertCosmeticParams) (AppPlayerCosmetic, error) {
	row := q.db.QueryRow(ctx, insertCosmetic,
		arg.PlayerID,
		arg.CosmeticID,
		arg.Source,
		arg.SourceRef,
		arg.ExpiresAt,
	)
	var i AppPlayerCosmetic
	err := row.Scan(
		&i.PlayerID,
		&i.CosmeticID,
		&i.Source,
		&i.SourceRef,
		&i.AcquiredAt,
		&i.ExpiresAt,
	)
	return i, err
}

const insertPlayerBoost = `-- name: InsertPlayerBoost :one
INSERT INTO app.player_boosts (player_id, bucket, amount_bp, starts_at, expires_at, source, source_ref)
VALUES ($1, $2, $3, $4,
        $5, $6, $7)
RETURNING id, player_id, bucket, amount_bp, starts_at, expires_at, source, source_ref
`

type InsertPlayerBoostParams struct {
	PlayerID  uuid.UUID
	Bucket    string
	AmountBp  int32
	StartsAt  time.Time
	ExpiresAt time.Time
	Source    string
	SourceRef *string
}

func (q *Queries) InsertPlayerBoost(ctx context.Context, arg InsertPlayerBoostParams) (AppPlayerBoost, error) {
	row := q.db.QueryRow(ctx, insertPlayerBoost,
		arg.PlayerID,
		arg.Bucket,
		arg.AmountBp,
		arg.StartsAt,
		arg.ExpiresAt,
		arg.Source,
		arg.SourceRef,
	)
	var i AppPlayerBoost
	err := row.Scan(
		&i.ID,
		&i.PlayerID,
		&i.Bucket,
		&i.AmountBp,
		&i.StartsAt,
		&i.ExpiresAt,
		&i.Source,
		&i.SourceRef,
	)
	return i, err
}

const listCosmetics = `-- name: ListCosmetics :many
SELECT cosmetic_id, source, acquired_at, expires_at FROM app.player_cosmetics
WHERE player_id = $1 ORDER BY acquired_at
`

type ListCosmeticsRow struct {
	CosmeticID string
	Source     string
	AcquiredAt time.Time
	ExpiresAt  *time.Time
}

func (q *Queries) ListCosmetics(ctx context.Context, playerID uuid.UUID) ([]ListCosmeticsRow, error) {
	rows, err := q.db.Query(ctx, listCosmetics, playerID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListCosmeticsRow{}
	for rows.Next() {
		var i ListCosmeticsRow
		if err := rows.Scan(
			&i.CosmeticID,
			&i.Source,
			&i.AcquiredAt,
			&i.ExpiresAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listLiveBoosts = `-- name: ListLiveBoosts :many
SELECT bucket, amount_bp, expires_at FROM app.player_boosts
WHERE player_id = $1
  AND starts_at <= $2::timestamptz
  AND expires_at > $2::timestamptz
`

type ListLiveBoostsParams struct {
	PlayerID uuid.UUID
	Now      time.Time
}

type ListLiveBoostsRow struct {
	Bucket    string
	AmountBp  int32
	ExpiresAt time.Time
}

func (q *Queries) ListLiveBoosts(ctx context.Context, arg ListLiveBoostsParams) ([]ListLiveBoostsRow, error) {
	rows, err := q.db.Query(ctx, listLiveBoosts, arg.PlayerID, arg.Now)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListLiveBoostsRow{}
	for rows.Next() {
		var i ListLiveBoostsRow
		if err := rows.Scan(&i.Bucket, &i.AmountBp, &i.ExpiresAt); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listTokens = `-- name: ListTokens :many
SELECT token, qty FROM app.player_tokens WHERE player_id = $1 AND qty > 0 ORDER BY token
`

type ListTokensRow struct {
	Token string
	Qty   int64
}

func (q *Queries) ListTokens(ctx context.Context, playerID uuid.UUID) ([]ListTokensRow, error) {
	rows, err := q.db.Query(ctx, listTokens, playerID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListTokensRow{}
	for rows.Next() {
		var i ListTokensRow
		if err := rows.Scan(&i.Token, &i.Qty); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const spendToken = `-- name: SpendToken :one
UPDATE app.player_tokens
SET qty = qty - $1::bigint, updated_at = now()
WHERE player_id = $2 AND token = $3 AND qty >= $1::bigint
RETURNING player_id, token, qty, updated_at
`

type SpendTokenParams struct {
	Qty      int64
	PlayerID uuid.UUID
	Token    string
}

// Uses tokens. Zero rows means the player does not hold that many, so the check
// and the spend are one statement and a double tap cannot use one twice.
func (q *Queries) SpendToken(ctx context.Context, arg SpendTokenParams) (AppPlayerToken, error) {
	row := q.db.QueryRow(ctx, spendToken, arg.Qty, arg.PlayerID, arg.Token)
	var i AppPlayerToken
	err := row.Scan(
		&i.PlayerID,
		&i.Token,
		&i.Qty,
		&i.UpdatedAt,
	)
	return i, err
}

const unwearLapsed = `-- name: UnwearLapsed :execrows
UPDATE app.players p SET
    cos_frame = CASE WHEN p.cos_frame = ANY($1::text[]) OR EXISTS (
                    SELECT 1 FROM app.player_cosmetics c WHERE c.player_id = p.id AND c.cosmetic_id = p.cos_frame
                      AND (c.expires_at IS NULL OR c.expires_at > $2::timestamptz))
                THEN p.cos_frame END,
    cos_title = CASE WHEN p.cos_title = ANY($1::text[]) OR EXISTS (
                    SELECT 1 FROM app.player_cosmetics c WHERE c.player_id = p.id AND c.cosmetic_id = p.cos_title
                      AND (c.expires_at IS NULL OR c.expires_at > $2::timestamptz))
                THEN p.cos_title END,
    cos_color = CASE WHEN p.cos_color = ANY($1::text[]) OR EXISTS (
                    SELECT 1 FROM app.player_cosmetics c WHERE c.player_id = p.id AND c.cosmetic_id = p.cos_color
                      AND (c.expires_at IS NULL OR c.expires_at > $2::timestamptz))
                THEN p.cos_color END,
    cos_crest = CASE WHEN p.cos_crest = ANY($1::text[]) OR EXISTS (
                    SELECT 1 FROM app.player_cosmetics c WHERE c.player_id = p.id AND c.cosmetic_id = p.cos_crest
                      AND (c.expires_at IS NULL OR c.expires_at > $2::timestamptz))
                THEN p.cos_crest END
WHERE ($3::uuid IS NULL OR p.id = $3::uuid)
  AND (p.cos_frame IS NOT NULL OR p.cos_title IS NOT NULL OR p.cos_color IS NOT NULL OR p.cos_crest IS NOT NULL)
  AND NOT (
      (p.cos_frame IS NULL OR p.cos_frame = ANY($1::text[]) OR EXISTS (
          SELECT 1 FROM app.player_cosmetics c WHERE c.player_id = p.id AND c.cosmetic_id = p.cos_frame
            AND (c.expires_at IS NULL OR c.expires_at > $2::timestamptz)))
      AND (p.cos_title IS NULL OR p.cos_title = ANY($1::text[]) OR EXISTS (
          SELECT 1 FROM app.player_cosmetics c WHERE c.player_id = p.id AND c.cosmetic_id = p.cos_title
            AND (c.expires_at IS NULL OR c.expires_at > $2::timestamptz)))
      AND (p.cos_color IS NULL OR p.cos_color = ANY($1::text[]) OR EXISTS (
          SELECT 1 FROM app.player_cosmetics c WHERE c.player_id = p.id AND c.cosmetic_id = p.cos_color
            AND (c.expires_at IS NULL OR c.expires_at > $2::timestamptz)))
      AND (p.cos_crest IS NULL OR p.cos_crest = ANY($1::text[]) OR EXISTS (
          SELECT 1 FROM app.player_cosmetics c WHERE c.player_id = p.id AND c.cosmetic_id = p.cos_crest
            AND (c.expires_at IS NULL OR c.expires_at > $2::timestamptz))))
`

type UnwearLapsedParams struct {
	Defaults []string
	Now      time.Time
	PlayerID *uuid.UUID
}

// Takes off whatever a lord wears but no longer holds: a patron's frame whose
// month ran out, a cosmetic a refund took back. Everyone's default crests have
// no row and are always held. The worn columns are what every list of other
// lords reads (raid cards, rankings, a kingdom's lords), so keeping them true
// here is what lets those lists skip a check per row. player_id NULL sweeps
// everyone (the cosmetics_lapse job); a player id tidies one lord at once.
func (q *Queries) UnwearLapsed(ctx context.Context, arg UnwearLapsedParams) (int64, error) {
	result, err := q.db.Exec(ctx, unwearLapsed, arg.Defaults, arg.Now, arg.PlayerID)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const upsertToken = `-- name: UpsertToken :one

INSERT INTO app.player_tokens (player_id, token, qty, updated_at)
VALUES ($1, $2, $3::bigint, now())
ON CONFLICT (player_id, token) DO UPDATE
SET qty = app.player_tokens.qty + EXCLUDED.qty, updated_at = now()
RETURNING player_id, token, qty, updated_at
`

type UpsertTokenParams struct {
	PlayerID uuid.UUID
	Token    string
	Qty      int64
}

// What service.grantBundle writes, beyond the columns the player row already has.
// See migration 00030.
func (q *Queries) UpsertToken(ctx context.Context, arg UpsertTokenParams) (AppPlayerToken, error) {
	row := q.db.QueryRow(ctx, upsertToken, arg.PlayerID, arg.Token, arg.Qty)
	var i AppPlayerToken
	err := row.Scan(
		&i.PlayerID,
		&i.Token,
		&i.Qty,
		&i.UpdatedAt,
	)
	return i, err
}

const wearCosmetic = `-- name: WearCosmetic :one
UPDATE app.players
SET cos_frame = CASE WHEN $1::text = 'frame' THEN $2 ELSE cos_frame END,
    cos_title = CASE WHEN $1::text = 'title' THEN $2 ELSE cos_title END,
    cos_color = CASE WHEN $1::text = 'name_color' THEN $2 ELSE cos_color END,
    cos_crest = CASE WHEN $1::text = 'crest' THEN $2 ELSE cos_crest END
WHERE id = $3
RETURNING id, username, display_name, level, xp, gold, treasury_gold, diamonds, energy_milli, energy_updated_at, stat_energy, stat_attack, stat_defense, stat_points_unspent, shield_until, action_seq, state, reset_offset_minutes, created_at, last_seen_at, soldier_slots, free_slot_claimed, free_recruit_claimed, is_bot, tax_milli_accrued, tax_updated_at, kingdom_id, kingdom_role, kingdom_joined_at, kingdom_donated_total, kingdom_favour, kingdom_rep_today, kingdom_donated_today, kingdom_day, avatar, tax_milli_per_hour, tax_unlogged, luck_bp, luck_expires_at, xp_boost_bp, xp_boost_expires_at, daily_streak, daily_claimed_on, might, legacy, kingdom_left_at, diamond_debt, refills_day, refills_used, boost_until, cos_frame, cos_title, cos_color, cos_crest, mail_bc_seen, vip_points, patron_until, steward_owned, bag_bonus, stipend_until, stipend_claimed, stipend_ref, vip_gift_on, shop_rerolls_day, shop_rerolls_used, storehouse_milli, storehouse_at, storehouse_cap_milli, cart_stock, cart_at, carts_opened, calendar_pos, calendar_cycle, frenzy_meter_milli, frenzy_last_at, frenzy_until, frenzy_energy_left, frenzy_day, frenzy_used, frenzy_ready_at, road_claimed, guide_step, guide_done_at, guide_skipped, winback_at, winback_tier, arena_day, arena_fights_used, arena_refresh_used, arena_first_win_on, bounty_day, bounty_placed, chat_muted_until, chat_strikes, chat_strike_at, chat_rules_version, chat_seen_seq, gift_day, gifts_taken, friend_req_day, friend_reqs, spy_day, spy_used, aid_day, aid_given, aid_asked_at, notif_raid, notif_chat, notif_mail, notif_events, notif_friends, quiet_from, quiet_to, privacy_profile, privacy_online, privacy_requests, talent_respecs
`

type WearCosmeticParams struct {
	Kind       string
	CosmeticID *string
	ID         uuid.UUID
}

// Wears a cosmetic of one kind, or takes it off (an empty id). The EXISTS keeps
// a lord from wearing what they do not own; the default crests are everyone's
// and are passed in as owned by the service.
func (q *Queries) WearCosmetic(ctx context.Context, arg WearCosmeticParams) (AppPlayer, error) {
	row := q.db.QueryRow(ctx, wearCosmetic, arg.Kind, arg.CosmeticID, arg.ID)
	var i AppPlayer
	err := row.Scan(
		&i.ID,
		&i.Username,
		&i.DisplayName,
		&i.Level,
		&i.Xp,
		&i.Gold,
		&i.TreasuryGold,
		&i.Diamonds,
		&i.EnergyMilli,
		&i.EnergyUpdatedAt,
		&i.StatEnergy,
		&i.StatAttack,
		&i.StatDefense,
		&i.StatPointsUnspent,
		&i.ShieldUntil,
		&i.ActionSeq,
		&i.State,
		&i.ResetOffsetMinutes,
		&i.CreatedAt,
		&i.LastSeenAt,
		&i.SoldierSlots,
		&i.FreeSlotClaimed,
		&i.FreeRecruitClaimed,
		&i.IsBot,
		&i.TaxMilliAccrued,
		&i.TaxUpdatedAt,
		&i.KingdomID,
		&i.KingdomRole,
		&i.KingdomJoinedAt,
		&i.KingdomDonatedTotal,
		&i.KingdomFavour,
		&i.KingdomRepToday,
		&i.KingdomDonatedToday,
		&i.KingdomDay,
		&i.Avatar,
		&i.TaxMilliPerHour,
		&i.TaxUnlogged,
		&i.LuckBp,
		&i.LuckExpiresAt,
		&i.XpBoostBp,
		&i.XpBoostExpiresAt,
		&i.DailyStreak,
		&i.DailyClaimedOn,
		&i.Might,
		&i.Legacy,
		&i.KingdomLeftAt,
		&i.DiamondDebt,
		&i.RefillsDay,
		&i.RefillsUsed,
		&i.BoostUntil,
		&i.CosFrame,
		&i.CosTitle,
		&i.CosColor,
		&i.CosCrest,
		&i.MailBcSeen,
		&i.VipPoints,
		&i.PatronUntil,
		&i.StewardOwned,
		&i.BagBonus,
		&i.StipendUntil,
		&i.StipendClaimed,
		&i.StipendRef,
		&i.VipGiftOn,
		&i.ShopRerollsDay,
		&i.ShopRerollsUsed,
		&i.StorehouseMilli,
		&i.StorehouseAt,
		&i.StorehouseCapMilli,
		&i.CartStock,
		&i.CartAt,
		&i.CartsOpened,
		&i.CalendarPos,
		&i.CalendarCycle,
		&i.FrenzyMeterMilli,
		&i.FrenzyLastAt,
		&i.FrenzyUntil,
		&i.FrenzyEnergyLeft,
		&i.FrenzyDay,
		&i.FrenzyUsed,
		&i.FrenzyReadyAt,
		&i.RoadClaimed,
		&i.GuideStep,
		&i.GuideDoneAt,
		&i.GuideSkipped,
		&i.WinbackAt,
		&i.WinbackTier,
		&i.ArenaDay,
		&i.ArenaFightsUsed,
		&i.ArenaRefreshUsed,
		&i.ArenaFirstWinOn,
		&i.BountyDay,
		&i.BountyPlaced,
		&i.ChatMutedUntil,
		&i.ChatStrikes,
		&i.ChatStrikeAt,
		&i.ChatRulesVersion,
		&i.ChatSeenSeq,
		&i.GiftDay,
		&i.GiftsTaken,
		&i.FriendReqDay,
		&i.FriendReqs,
		&i.SpyDay,
		&i.SpyUsed,
		&i.AidDay,
		&i.AidGiven,
		&i.AidAskedAt,
		&i.NotifRaid,
		&i.NotifChat,
		&i.NotifMail,
		&i.NotifEvents,
		&i.NotifFriends,
		&i.QuietFrom,
		&i.QuietTo,
		&i.PrivacyProfile,
		&i.PrivacyOnline,
		&i.PrivacyRequests,
		&i.TalentRespecs,
	)
	return i, err
}
